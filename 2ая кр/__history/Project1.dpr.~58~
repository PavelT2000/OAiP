program Project1;

{$APPTYPE CONSOLE}
{$R *.res}

uses
  System.SysUtils;

Type
  Tmas = array [1 .. 18] of real;
  Tmatrix = array [1 .. 3, 1 .. 18] of real;

procedure fillMas(var mas: Tmas);
begin
  for var I := Low(mas) to High(mas) do
  begin
    mas[I] := (Random(30) - 15) / 10;
  end;

end;

procedure changeMatrix(var mat: Tmatrix; var mas: Tmas);
begin
  var
    eps, y, x,elem,sum: real;
  var n:integer;
  eps := 0.01;

  for var I := 1 to 3 do
  begin
    for var j := Low(mas) to High(mas) do
    begin
      x:=mas[j];
      sum:=1;
      elem:=x*x/2*-1;
      n:=2;
      sum:=sum+elem;
      n:=n+2;
      elem:=elem*x*x/n/(n-1)*-1;
      while (abs(elem)>eps) do
      begin
        sum:=sum+elem;
        n:=n+2;
        elem:=elem*x*x/n/(n-1)*-1
      end;
      mat[i,j]:=sum;
    end;
    eps := eps * 0.01;
  end;
end;
procedure OutMatrix(var mat:Tmatrix);
begin
  for var i := 1 to 3 do
  begin
    for var j := 1 to 18 do
    begin
      Write(mat[i,j]:7:5,' ');
    end;
    writeln;
  end;
end;

begin
  var
    mas: Tmas;
  var
    mat: Tmatrix;

  fillMas(mas);
  changeMatrix(mat, mas);
  OutMatrix(mat);

  readln;

end.
