program Laba7;

{$APPTYPE CONSOLE}
{$R *.res}

uses
  System.SysUtils;

begin
  var
    str, lastWord: string;
  readln(str);
  var
  i := length(str);
  var
    check: boolean;
  check := true;
  while (str[i] <> ' ') and (i<>0) do
    Dec(i);
  lastWord:=str.Substring(i,Length(str)-i);



  readln;


end.
